/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export',
    compiler: {
      styledComponents: true,
    },
    basePath: '/github-pages',
  };

module.exports = nextConfig
